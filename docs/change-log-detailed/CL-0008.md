# CL-0008: Fix parent relativePath formatting and dependency BOMs

## Summary
This change corrects malformed module POM parent entries and adds BOMs for AWS SDK and gRPC dependencies.

## Motivation
The Maven build must parse module POMs correctly and resolve managed dependency versions.

## Changes
- Fixed parent relativePath formatting in module POMs.
- Added AWS SDK and gRPC BOMs and Spring Boot plugin version to parent POM.

## Verification
- Pending: `mvn -q -DskipTests=false verify`.

## Risks
- Low risk; updates only build configuration.

## Rollback
- Revert parent POM BOM entries and relativePath fixes.

## Notes
- Use parent BOMs to manage dependency versions across modules.

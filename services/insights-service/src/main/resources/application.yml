server:
  port: 8084
spring:
  application:
    name: insights-service
  kafka:
    bootstrap-servers: localhost:9092
  rabbitmq:
    host: localhost
    port: 5672
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
